exp:
  project:
  run_name: 

params:
  lr: 0.001
  seed: 0
  epochs: 100
  batch: 64
  # vqvae params
  in_out_channels: 1
  hidden_dim: 128
  latent_dim: 256
  num_embeddings: 512
  embedding_dim: 256
  commitment_cost: 0.25